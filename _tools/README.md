# 🚀 文章自動化生成工具包

這個工具包幫助你快速生成高品質的部落格文章，節省 60% 以上的寫作時間！

## 📦 工具列表

### 1. `article_generator.py` - 文章框架生成器
快速生成文章的基礎框架，包含完整的結構和 TODO 標記。

### 2. `ai_content_filler.py` - AI 內容填充工具
使用 Claude API 自動填充文章中的 TODO 部分。

### 3. `quick_publish.sh` - 一鍵發布腳本
自動化文章發布流程：移動文件、commit、push。

---

## 🎯 快速開始

### 安裝依賴

```bash
cd _tools
pip install anthropic pyyaml
```

### 設定 API Key（可選）

如果你想使用 AI 自動填充功能：

```bash
export ANTHROPIC_API_KEY="your-api-key-here"
```

或者在 `.env` 文件中設定（推薦）：

```bash
echo "ANTHROPIC_API_KEY=your-api-key-here" > .env
```

---

## 📝 使用方法

### 方法一：完全自動化（推薦！）

**步驟 1：生成文章框架**

```bash
# 生成對比類文章（如 Claude vs ChatGPT）
python article_generator.py \
  --template comparison \
  --title "Claude vs ChatGPT：2025 年終極對比" \
  --item-a "Claude" \
  --item-b "ChatGPT" \
  --categories "AI工具" "工具評測" \
  --tags "Claude" "ChatGPT" "AI對比"

# 生成工具評測文章（如 Notion AI）
python article_generator.py \
  --template tool_review \
  --title "Notion AI 完整指南" \
  --tool "Notion AI" \
  --categories "AI工具" "生產力" \
  --tags "Notion" "AI" "知識管理"

# 生成快速筆記
python article_generator.py \
  --template quick_note \
  --title "今天學到的 Python 技巧" \
  --categories "技術筆記" \
  --tags "Python" "開發技巧"
```

**步驟 2：分析文章完成度**

```bash
python ai_content_filler.py --file ../_drafts/2025-11-14-claude-vs-chatgpt.md --analyze
```

輸出範例：
```
📊 文章分析報告
==================================================
標題：Claude vs ChatGPT：2025 年終極對比
總行數：234
總字數：3456
待完成項目：15
完成度：75.5%

待完成項目：
1. 填寫主要特色 1
2. 填寫主要特色 2
...
```

**步驟 3：AI 自動填充內容**

```bash
# 互動模式（逐個確認）
python ai_content_filler.py --file ../_drafts/2025-11-14-claude-vs-chatgpt.md

# 自動模式（全部自動填充）
python ai_content_filler.py --file ../_drafts/2025-11-14-claude-vs-chatgpt.md --auto
```

**步驟 4：手動編輯優化**

在 AI 填充的基礎上，加入你的個人經驗和見解。

**步驟 5：發布文章**

```bash
# 移動到 _posts 目錄並發布
bash quick_publish.sh ../_drafts/2025-11-14-claude-vs-chatgpt_filled.md "新增 Claude vs ChatGPT 對比文章"
```

---

### 方法二：純手動（不使用 AI）

```bash
# 1. 生成框架
python article_generator.py --template comparison --title "我的文章" --item-a "A" --item-b "B"

# 2. 手動填寫所有 [ ] TODO 部分
# 使用你喜歡的編輯器

# 3. 發布
bash quick_publish.sh ../_drafts/2025-11-14-my-article.md "新增文章"
```

---

## 💡 實用範例

### 範例 1：快速寫一篇 AI 工具對比

```bash
# 生成框架（30 秒）
python article_generator.py -t comparison \
  --title "Cursor vs GitHub Copilot：程式開發 AI 助手對比" \
  --item-a "Cursor" --item-b "GitHub Copilot" \
  --categories "AI工具" "程式開發" \
  --tags "Cursor" "GitHub Copilot" "AI編程"

# AI 填充（5 分鐘）
python ai_content_filler.py -f ../_drafts/2025-11-14-cursor-vs-github-copilot.md --auto

# 手動優化（30 分鐘）
# 加入個人經驗、截圖、實測數據

# 發布（1 分鐘）
bash quick_publish.sh ../_drafts/2025-11-14-cursor-vs-github-copilot_filled.md "新增 Cursor vs Copilot 對比"
```

**總耗時：約 40 分鐘（傳統方式需要 3-4 小時）**

### 範例 2：每日快速筆記

```bash
# 生成筆記框架
python article_generator.py -t quick_note \
  --title "解決 Jekyll 構建錯誤的方法" \
  --categories "技術筆記" \
  --tags "Jekyll" "除錯"

# 手動填寫（15 分鐘）
# 記錄你今天遇到的問題和解決方法

# 發布
bash quick_publish.sh ../_drafts/2025-11-14-jekyll-build-error.md "新增技術筆記"
```

**總耗時：約 20 分鐘**

---

## 🎨 文章模板說明

### 1. `comparison` - 對比類文章

**適合內容：**
- AI 工具對比（Claude vs ChatGPT）
- 技術方案對比（React vs Vue）
- 產品評測對比

**結構：**
- 基礎認識
- 核心能力對比
- 使用場景建議
- 優劣勢總結
- 選擇建議

**預估字數：** 4000-6000 字

### 2. `tool_review` - 工具評測

**適合內容：**
- 單一工具深度評測
- 使用指南
- 最佳實踐分享

**結構：**
- 工具簡介
- 核心功能解析
- 實戰應用場景
- 進階技巧
- 總結建議

**預估字數：** 3000-4000 字

### 3. `quick_note` - 快速筆記

**適合內容：**
- 技術問題解決方案
- 學習心得
- 工具使用技巧

**結構：**
- 核心觀點
- 詳細說明
- 實際應用
- 關鍵要點

**預估字數：** 500-800 字

---

## ⚙️ 進階配置

### 自定義模板

你可以修改 `article_generator.py` 中的模板方法，創建自己的模板：

```python
def generate_my_custom_template(self, config):
    """自定義模板"""
    # 你的模板內容
    return template
```

### 調整 AI 生成參數

編輯 `ai_content_filler.py` 中的 prompt：

```python
prompt = f"""你是一個專業的繁體中文技術寫作助手。

# 在這裡加入你的自定義指令
# 例如：使用更口語化的語言
# 或是：加入更多技術細節

...
"""
```

### 批量處理

```bash
# 批量分析所有草稿
for file in ../_drafts/*.md; do
    python ai_content_filler.py --file "$file" --analyze
done
```

---

## 📊 效率對比

### 傳統方式 vs 自動化工具

| 步驟 | 傳統方式 | 使用工具 | 節省時間 |
|------|---------|---------|---------|
| 構思大綱 | 30 分鐘 | 1 分鐘 | -97% |
| 撰寫框架 | 45 分鐘 | 自動生成 | -100% |
| 填充內容 | 120 分鐘 | 5 分鐘（AI）+ 30 分鐘（優化） | -71% |
| 格式調整 | 20 分鐘 | 自動完成 | -100% |
| 發布流程 | 10 分鐘 | 1 分鐘 | -90% |
| **總計** | **225 分鐘** | **40 分鐘** | **-82%** |

---

## 🔧 故障排除

### 問題 1：API Key 錯誤

```bash
# 檢查 API Key 是否設定
echo $ANTHROPIC_API_KEY

# 或使用 --api-key 參數直接傳入
python ai_content_filler.py --file article.md --api-key "your-key"
```

### 問題 2：生成的文件找不到

```bash
# 確認文件路徑
ls -la ../_drafts/

# 使用絕對路徑
python article_generator.py --template comparison --title "test"
```

### 問題 3：AI 生成內容不理想

**解決方法：**
1. 使用互動模式逐個檢查：去掉 `--auto` 參數
2. 手動編輯 AI 生成的內容
3. 調整 prompt 讓 AI 更了解你的風格

---

## 💰 成本估算

### 使用 Claude API 的費用

**假設：**
- 一篇文章有 15 個 TODO 項目
- 每個 TODO 生成約 200 tokens
- Claude API 價格：$3 / 1M input tokens, $15 / 1M output tokens

**計算：**
- Input: ~500 tokens × 15 = 7,500 tokens
- Output: ~200 tokens × 15 = 3,000 tokens
- 成本：(7500 × $3 + 3000 × $15) / 1,000,000 ≈ **$0.07/篇**

**結論：** 每篇文章成本不到 NT$3，超級划算！

---

## 🚀 未來計劃

- [ ] 支援更多文章模板
- [ ] 整合 SEO 優化建議
- [ ] 自動生成配圖（DALL-E）
- [ ] 文章品質評分系統
- [ ] 一鍵發布到多個平台
- [ ] 自動內部連結建議

---

## 📞 需要幫助？

1. 查看範例文章：`../_drafts/*-outline.md`
2. 參考現有文章：`../_posts/*.md`
3. 提交 Issue 到 GitHub

---

## 🎉 開始使用

現在就試試看：

```bash
python article_generator.py -t quick_note --title "測試我的第一篇自動生成文章" --categories "測試"
```

祝你寫作愉快！ ✍️
